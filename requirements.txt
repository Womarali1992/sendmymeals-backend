# FastAPI and server dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic>=2.0.0

# Machine learning dependencies (torch installed separately in Dockerfile for CPU-only)
transformers>=4.36.0
pillow>=10.0.0
huggingface-hub>=0.20.0

# Additional utilities
python-dotenv>=1.0.0
httpx>=0.25.0
